import { FC } from "react"

interface NavigationProps { }

export const Navigation: FC<NavigationProps> = () => {
  return <nav className="max-w-60 w-full h-full rounded-[14px] bg-white lg:inline-flex hidden">Navigation</nav>
}
